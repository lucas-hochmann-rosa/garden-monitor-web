import type { NextConfig } from 'next';

// Configuração padrão do Next.js - build otimizado para deploy na Vercel.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
